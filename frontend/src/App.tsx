import './App.css';

function App() {
  return (
    <main className="app-shell">
      <section className="status-panel" aria-labelledby="app-title">
        <p className="eyebrow">Monorepo core setup</p>
        <h1 id="app-title">PROJECT OOH-AHH is running.</h1>
        <p className="status-text">
          The frontend shell is ready for future visualization, tracing,
          validation, and mentor features.
        </p>
      </section>
    </main>
  );
}

export default App;
